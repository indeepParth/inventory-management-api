# Public backend API base URL used by the browser app.
# For local development, copy this file to .env.local.
# For production builds, set this value in the deployment environment or CI.
# Do not put secrets in VITE_ variables; Vite embeds them into browser assets.
VITE_API_BASE_URL=https://localhost:5001
